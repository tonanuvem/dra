# Backend Environment Variables

# Supabase Configuration
# SUPABASE_URL=your_supabase_url
# SUPABASE_PUBLISHABLE_KEY=sb_publishable_...   # chave pública (leitura no frontend)
# SUPABASE_SECRET_KEY=sb_secret_...             # secret key (escrita pelo backend — bypassa RLS)

# Application Settings
PORT=8501
